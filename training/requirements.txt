# Core dependencies — required for both simple and full pipeline
numpy>=1.24.0
scipy>=1.10.0
soundfile>=0.12.0

# Required only for full pipeline (NN training + MRSTFT fine-tuning)
torch>=2.6.0

# Optional — used by generate.py and analysis scripts
matplotlib>=3.7.0
